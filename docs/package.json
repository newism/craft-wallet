{
  "name": "@newism/craft-wallet-docs",
  "private": true,
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "deploy": "vitepress build && wrangler pages deploy .vitepress/dist --project-name=newism-craft-wallet --branch=main"
  },
  "dependencies": {
    "@newism/vitepress-shared": "file:../../docs"
  },
  "devDependencies": {
    "vitepress": "^2.0.0-alpha.17"
  }
}
